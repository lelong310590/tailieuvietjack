import * as types from './../const/ActionType';

let initialState = {
	files: []
};

let UploadReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default UploadReducer;